# Simple rapidlib regression example

Builds a neural net, trains it with input and output vectors then performs inference.

```
mkdir build
cd build
cmake .. 
make
./rapidlib-regression-example
```

